/** @type {import('next-sitemap').IConfig} */
const config = {
    siteUrl: 'https://0xticket.vercel.app/',
    generateRobotsTxt: true, // (optional)
    // ...other options
  }

  module.exports = config;